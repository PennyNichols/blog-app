import React from 'react'


const Details = () => {
    // const { blogs } = useContext(BlogContext);
    // const {id} = useParams()
    return (
    <div>blog.title</div>
  )
}

export default Details